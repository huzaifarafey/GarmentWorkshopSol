@model GarmentWorkshop.Models.Worker

@{
    ViewData["Title"] = "Edit Worker";
}

<div class="gw-page-header">
    <div>
        <h2>Edit Worker</h2>
        <p class="gw-page-subtitle">Update details for this worker.</p>
    </div>
    <div class="gw-page-actions">
        <a asp-action="Index" class="btn btn-outline-primary"><i class="bi bi-arrow-left"></i> Back to List</a>
    </div>
</div>

<div class="gw-card gw-form-card">
    <div class="gw-form-card-head">
        <span class="gw-form-card-icon"><i class="bi bi-person-gear"></i></span>
        <div>
            <strong>Worker Details</strong>
            <span>Update information about the worker</span>
        </div>
    </div>
    <form asp-action="Edit" method="post" class="gw-form-body">
        <input type="hidden" asp-for="Id" />

        <div class="row g-3">
            <div class="col-md-6 gw-field">
                <label class="form-label">Name</label>
                <div class="gw-field-icon">
                    <i class="bi bi-person"></i>
                    <input asp-for="Name" class="form-control" />
                </div>
                <span asp-validation-for="Name" class="text-danger"></span>
            </div>

            <div class="col-md-6 gw-field">
                <label class="form-label">Phone</label>
                <div class="gw-field-icon">
                    <i class="bi bi-telephone"></i>
                    <input asp-for="Phone" class="form-control" />
                </div>
                <span asp-validation-for="Phone" class="text-danger"></span>
            </div>

            <div class="col-md-6 gw-field">
                <label class="form-label">Status</label>
                <div class="gw-field-icon">
                    <i class="bi bi-toggle2-on"></i>
                    <select asp-for="Status" class="form-select" asp-items="Html.GetEnumSelectList<GarmentWorkshop.Models.WorkerStatus>()"></select>
                </div>
            </div>
        </div>

        <div class="gw-form-actions">
            <a asp-action="Index" class="btn btn-secondary"><i class="bi bi-x-lg"></i> Cancel</a>
            <button type="submit" class="btn btn-primary"><i class="bi bi-check2"></i> Save</button>
        </div>
    </form>
</div>
